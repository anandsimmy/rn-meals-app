import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  gridTileContainer: {
    height: 150,
    width: '50%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  foodText: {
    fontWeight: '500',
  },
});
